# LLM_fine_tuning
Self explanatory if you look at the name of the repo. But just in case you're interested, I am just trying to check if fine-tuning a small-sized LLM (with <3--5B parameters) using LoRA/QLoRA changes the performance of the already pre-trained models. 
